function GameCard() {

    return (
        <button
            className="game-card"
            type={"button"}
            onClick={() => {}}
        >
            temp
        </button>

    )
}

export default GameCard